{% extends "layout.twig" %}

{% block title %}アカウント登録{% endblock %}

{% block content %}
<h2>アカウント登録</h2>

<form action="{{ base_url }}/account/register" method="POST">
    <input type="hidden" name="_token" value="{{ _token|e }}" />

    {% include "components/error_list.twig" %}

    <table>
        <tbody>
            <tr>
                <th>ユーザID</th>
                <td>
                    <input type="text" name="user_name" value="{{ user_name|e }}" />
                </td>
            </tr>
            <tr>
                <th>パスワード</th>
                <td>
                    <input type="password" name="password" value="{{ password|e }}" />
                </td>
            </tr>
        </tbody>
    </table>

    <p>
        <input type="submit" value="登録" />
    </p>
</form>
{% endblock %}
